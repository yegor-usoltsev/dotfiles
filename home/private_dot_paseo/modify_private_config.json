#!/bin/sh
set -eu

# Preserve Paseo's generated IDs, credentials, providers, and unrelated settings.
jq -s '
    (.[0] // {})
    | .version //= 1
    | .daemon.mcp.enabled = true
    | .daemon.mcp.injectIntoAgents = false
    | del(.daemon.appendSystemPrompt)
    '
